# 🧭 Bootstrap Breakpoints

## What Are Breakpoints?

**Breakpoints** are predefined screen widths at which the layout of a webpage changes to provide the best viewing experience across different devices — from mobile phones to large desktops.

Bootstrap uses **CSS media queries** to apply different styles at specific viewport widths.  
This helps in making a **responsive design** that adapts automatically to different devices.

---

## 📱 Default Bootstrap Breakpoints

| Breakpoint  | Class Prefix | Screen Size (min-width) | Typical Device |
|-------------|--------------|-------------------------|----------------|
| Extra small | `None`       | `<576px`                | Mobile phones  |
| Small       | `sm`         | `≥576px`                | Large phones   |
| Medium      | `md`         | `≥768px`                | Tablets        |
| Large       | `lg`         | `≥992px`                | Laptops        |
| Extra large | `xl`         | `≥1200px`               | Desktops       |
| XXL         | `xxl`        | `≥1400px`               | Large screens  |

---

## 🧩 Example: Responsive Grid Layout

Here’s how you can use breakpoints with Bootstrap’s grid system:

```html
<div class="container">
  <div class="row">
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      <div class="p-3 bg-primary text-white text-center">Column 1</div>
    </div>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      <div class="p-3 bg-success text-white text-center">Column 2</div>
    </div>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      <div class="p-3 bg-warning text-white text-center">Column 3</div>
    </div>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      <div class="p-3 bg-danger text-white text-center">Column 4</div>
    </div>
  </div>
</div>
```

### 🔍 Explanation

- `col-12` → Full width on extra small devices (<576px)
- `col-sm-6` → Two columns on small devices (≥576px)
- `col-md-4` → Three columns on medium devices (≥768px)
- `col-lg-3` → Four columns on large devices (≥992px)

---

## 🧠 How It Works

Behind the scenes, Bootstrap uses **CSS media queries** like this:

```css
@media (min-width: 768px) {
  /* Apply styles for tablets and above */
}
@media (min-width: 992px) {
  /* Apply styles for laptops and above */
}
```

This ensures elements adjust automatically as the screen size changes.

---

## 🧾 Summary

- Breakpoints define **where your layout adapts** to screen width.
- Bootstrap uses **mobile-first design**, meaning styles apply first to smaller screens.
- Classes like `col-md-4`, `d-lg-block`, or `text-sm-center` depend on these breakpoints.

---

## ✅ Quick Tip

You can customize breakpoints in Bootstrap’s SCSS variables:

```scss
$grid-breakpoints: (
  xs: 0,
  sm: 576px,
  md: 768px,
  lg: 992px,
  xl: 1200px,
  xxl: 1400px
);
```

Change them to match your project’s responsive needs.
