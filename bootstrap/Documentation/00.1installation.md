# 🧱 Bootstrap Installation Guide (via npm)

This guide explains how to install **Bootstrap** using **npm**, organize your project structure, and create a simple HTML file to test it.

---

## 📦 Step 1: Initialize a New Project

First, create a new folder for your project and initialize npm.

```bash
mkdir bootstrap-demo
cd bootstrap-demo
npm init -y
```

This will generate a default `package.json` file.

---

## 🚀 Step 2: Install Bootstrap and Dependencies

Bootstrap requires **Popper** (for tooltips, dropdowns, etc.) and optionally **Sass** if you plan to customize the theme.

```bash
# Install Bootstrap and Popper
npm install bootstrap @popperjs/core
```

Optionally, install Sass for custom builds:

```bash
npm install sass --save-dev
```

---

## 📁 Step 3: Folder Structure

After installation, your project folder should look like this:

```
bootstrap-demo/
├── node_modules/           # Installed npm packages
├── package.json            # npm configuration file
├── package-lock.json       # Auto-generated dependency lock file
├── index.html              # Demo HTML file
└── assets/
    ├── css/                # Custom CSS files
    └── js/                 # Custom JS files
```

---

## 🧩 Step 4: Using Bootstrap in HTML

Now create a file named `index.html` in your project root and add the following content:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bootstrap Demo</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="./node_modules/bootstrap/dist/css/bootstrap.min.css">
</head>
<body>

  <div class="container text-center mt-5">
    <h1 class="text-primary">Hello, Bootstrap!</h1>
    <p class="lead">This is a simple demo using Bootstrap installed via npm.</p>
    <button class="btn btn-success">Click Me</button>
  </div>

  <!-- Bootstrap JS + Popper -->
  <script src="./node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

---

## 🧠 Step 5: Run the Project

Simply open the `index.html` file in your browser.  
You should see a centered title, paragraph, and green button styled with Bootstrap.

> 💡 Tip: For development with live reload, you can use any local server (e.g., `npx serve`, VS Code’s Live Server, or `http-server`).

```bash
# Example using serve
npx serve .
```

---

## ⚙️ Optional: Customize Bootstrap (using Sass)

If you installed `sass`, you can import Bootstrap’s SCSS files and override variables:

```scss
/* assets/css/custom.scss */
$primary: #4b0082;  // Change primary color to indigo
@import "../node_modules/bootstrap/scss/bootstrap";
```

Then compile it:

```bash
npx sass assets/css/custom.scss assets/css/custom.css
```

And update your HTML:

```html
<link rel="stylesheet" href="assets/css/custom.css">
```

---

## ✅ Summary

| Step | Description |
|------|--------------|
| 1 | Initialize npm project |
| 2 | Install Bootstrap and Popper |
| 3 | Create folder structure |
| 4 | Build basic HTML using Bootstrap |
| 5 | (Optional) Customize with Sass |

---

**Author:** *Learn Bootstrap Project*  
**Version:** 1.0  
**License:** MIT
